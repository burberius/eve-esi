/*
 * EVE Swagger Interface
 * An OpenAPI for EVE Online
 *
 * The version of the OpenAPI document: 1.33
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package net.troja.eve.esi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import net.troja.eve.esi.model.FactionWarfareLeaderboardActiveTotalKills;
import net.troja.eve.esi.model.FactionWarfareLeaderboardLastWeekKills;
import net.troja.eve.esi.model.FactionWarfareLeaderboardYesterdayKills;
import java.io.Serializable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import net.troja.eve.esi.JSON;

/**
 * Top 4 rankings of factions by number of kills from yesterday, last week and
 * in total
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.12.0")
public class FactionWarfareLeaderboardKills implements Serializable {
    private static final long serialVersionUID = 1L;

    public static final String SERIALIZED_NAME_ACTIVE_TOTAL = "active_total";
    @SerializedName(SERIALIZED_NAME_ACTIVE_TOTAL)
    @javax.annotation.Nonnull
    private List<FactionWarfareLeaderboardActiveTotalKills> activeTotal;

    public static final String SERIALIZED_NAME_LAST_WEEK = "last_week";
    @SerializedName(SERIALIZED_NAME_LAST_WEEK)
    @javax.annotation.Nonnull
    private List<FactionWarfareLeaderboardLastWeekKills> lastWeek;

    public static final String SERIALIZED_NAME_YESTERDAY = "yesterday";
    @SerializedName(SERIALIZED_NAME_YESTERDAY)
    @javax.annotation.Nonnull
    private List<FactionWarfareLeaderboardYesterdayKills> yesterday;

    public FactionWarfareLeaderboardKills() {
    }

    public FactionWarfareLeaderboardKills activeTotal(
            @javax.annotation.Nonnull List<FactionWarfareLeaderboardActiveTotalKills> activeTotal) {
        this.activeTotal = activeTotal;
        return this;
    }

    public FactionWarfareLeaderboardKills addActiveTotalItem(FactionWarfareLeaderboardActiveTotalKills activeTotalItem) {
        if (this.activeTotal == null) {
            this.activeTotal = new ArrayList<>();
        }

        this.activeTotal.add(activeTotalItem);
        return this;
    }

    /**
     * Top 4 ranking of factions active in faction warfare by total kills. A
     * faction is considered \&quot;active\&quot; if they have participated in
     * faction warfare in the past 14 days
     * 
     * @return activeTotal
     */
    @javax.annotation.Nonnull
    public List<FactionWarfareLeaderboardActiveTotalKills> getActiveTotal() {
        return activeTotal;
    }

    public void setActiveTotal(@javax.annotation.Nonnull List<FactionWarfareLeaderboardActiveTotalKills> activeTotal) {
        this.activeTotal = activeTotal;
    }

    public FactionWarfareLeaderboardKills lastWeek(
            @javax.annotation.Nonnull List<FactionWarfareLeaderboardLastWeekKills> lastWeek) {
        this.lastWeek = lastWeek;
        return this;
    }

    public FactionWarfareLeaderboardKills addLastWeekItem(FactionWarfareLeaderboardLastWeekKills lastWeekItem) {
        if (this.lastWeek == null) {
            this.lastWeek = new ArrayList<>();
        }

        this.lastWeek.add(lastWeekItem);
        return this;
    }

    /**
     * Top 4 ranking of factions by kills in the past week
     * 
     * @return lastWeek
     */
    @javax.annotation.Nonnull
    public List<FactionWarfareLeaderboardLastWeekKills> getLastWeek() {
        return lastWeek;
    }

    public void setLastWeek(@javax.annotation.Nonnull List<FactionWarfareLeaderboardLastWeekKills> lastWeek) {
        this.lastWeek = lastWeek;
    }

    public FactionWarfareLeaderboardKills yesterday(
            @javax.annotation.Nonnull List<FactionWarfareLeaderboardYesterdayKills> yesterday) {
        this.yesterday = yesterday;
        return this;
    }

    public FactionWarfareLeaderboardKills addYesterdayItem(FactionWarfareLeaderboardYesterdayKills yesterdayItem) {
        if (this.yesterday == null) {
            this.yesterday = new ArrayList<>();
        }

        this.yesterday.add(yesterdayItem);
        return this;
    }

    /**
     * Top 4 ranking of factions by kills in the past day
     * 
     * @return yesterday
     */
    @javax.annotation.Nonnull
    public List<FactionWarfareLeaderboardYesterdayKills> getYesterday() {
        return yesterday;
    }

    public void setYesterday(@javax.annotation.Nonnull List<FactionWarfareLeaderboardYesterdayKills> yesterday) {
        this.yesterday = yesterday;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        FactionWarfareLeaderboardKills factionWarfareLeaderboardKills = (FactionWarfareLeaderboardKills) o;
        return Objects.equals(this.activeTotal, factionWarfareLeaderboardKills.activeTotal)
                && Objects.equals(this.lastWeek, factionWarfareLeaderboardKills.lastWeek)
                && Objects.equals(this.yesterday, factionWarfareLeaderboardKills.yesterday);
    }

    @Override
    public int hashCode() {
        return Objects.hash(activeTotal, lastWeek, yesterday);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class FactionWarfareLeaderboardKills {\n");
        sb.append("    activeTotal: ").append(toIndentedString(activeTotal)).append("\n");
        sb.append("    lastWeek: ").append(toIndentedString(lastWeek)).append("\n");
        sb.append("    yesterday: ").append(toIndentedString(yesterday)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("active_total");
        openapiFields.add("last_week");
        openapiFields.add("yesterday");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("active_total");
        openapiRequiredFields.add("last_week");
        openapiRequiredFields.add("yesterday");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement
     *            JSON Element
     * @throws IOException
     *             if the JSON Element is invalid with respect to
     *             FactionWarfareLeaderboardKills
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!FactionWarfareLeaderboardKills.openapiRequiredFields.isEmpty()) { // has
                                                                                   // required
                                                                                   // fields
                                                                                   // but
                                                                                   // JSON
                                                                                   // element
                                                                                   // is
                                                                                   // null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in FactionWarfareLeaderboardKills is not found in the empty JSON string",
                                FactionWarfareLeaderboardKills.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!FactionWarfareLeaderboardKills.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the `FactionWarfareLeaderboardKills` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the
        // JSON string
        for (String requiredField : FactionWarfareLeaderboardKills.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s", requiredField,
                        jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("active_total").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `active_total` to be an array in the JSON string but got `%s`",
                    jsonObj.get("active_total").toString()));
        }

        JsonArray jsonArrayactiveTotal = jsonObj.getAsJsonArray("active_total");
        // validate the required field `active_total` (array)
        for (int i = 0; i < jsonArrayactiveTotal.size(); i++) {
            FactionWarfareLeaderboardActiveTotalKills.validateJsonElement(jsonArrayactiveTotal.get(i));
        };
        // ensure the json data is an array
        if (!jsonObj.get("last_week").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `last_week` to be an array in the JSON string but got `%s`",
                    jsonObj.get("last_week").toString()));
        }

        JsonArray jsonArraylastWeek = jsonObj.getAsJsonArray("last_week");
        // validate the required field `last_week` (array)
        for (int i = 0; i < jsonArraylastWeek.size(); i++) {
            FactionWarfareLeaderboardLastWeekKills.validateJsonElement(jsonArraylastWeek.get(i));
        };
        // ensure the json data is an array
        if (!jsonObj.get("yesterday").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `yesterday` to be an array in the JSON string but got `%s`",
                    jsonObj.get("yesterday").toString()));
        }

        JsonArray jsonArrayyesterday = jsonObj.getAsJsonArray("yesterday");
        // validate the required field `yesterday` (array)
        for (int i = 0; i < jsonArrayyesterday.size(); i++) {
            FactionWarfareLeaderboardYesterdayKills.validateJsonElement(jsonArrayyesterday.get(i));
        };
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!FactionWarfareLeaderboardKills.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes
                             // 'FactionWarfareLeaderboardKills' and its
                             // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<FactionWarfareLeaderboardKills> thisAdapter = gson.getDelegateAdapter(this,
                    TypeToken.get(FactionWarfareLeaderboardKills.class));

            return (TypeAdapter<T>) new TypeAdapter<FactionWarfareLeaderboardKills>() {
                @Override
                public void write(JsonWriter out, FactionWarfareLeaderboardKills value) throws IOException {
                    JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                    elementAdapter.write(out, obj);
                }

                @Override
                public FactionWarfareLeaderboardKills read(JsonReader in) throws IOException {
                    JsonElement jsonElement = elementAdapter.read(in);
                    validateJsonElement(jsonElement);
                    return thisAdapter.fromJsonTree(jsonElement);
                }

            }.nullSafe();
        }
    }

    /**
     * Create an instance of FactionWarfareLeaderboardKills given an JSON string
     *
     * @param jsonString
     *            JSON string
     * @return An instance of FactionWarfareLeaderboardKills
     * @throws IOException
     *             if the JSON string is invalid with respect to
     *             FactionWarfareLeaderboardKills
     */
    public static FactionWarfareLeaderboardKills fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, FactionWarfareLeaderboardKills.class);
    }

    /**
     * Convert an instance of FactionWarfareLeaderboardKills to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
