/*
 * EVE Stable Infrastructure (ESI) - tranquility
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2020-01-01
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package net.troja.eve.esi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import java.io.Serializable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import net.troja.eve.esi.JSON;

/**
 * Coordinates of the structure in Cartesian space relative to the Sun, in
 * metres.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.15.0")
public class UniverseStructureResponsePosition implements Serializable {
    private static final long serialVersionUID = 1L;

    public static final String SERIALIZED_NAME_X = "x";
    @SerializedName(SERIALIZED_NAME_X)
    @javax.annotation.Nonnull
    private Double x;

    public static final String SERIALIZED_NAME_Y = "y";
    @SerializedName(SERIALIZED_NAME_Y)
    @javax.annotation.Nonnull
    private Double y;

    public static final String SERIALIZED_NAME_Z = "z";
    @SerializedName(SERIALIZED_NAME_Z)
    @javax.annotation.Nonnull
    private Double z;

    public UniverseStructureResponsePosition() {
    }

    public UniverseStructureResponsePosition x(@javax.annotation.Nonnull Double x) {
        this.x = x;
        return this;
    }

    /**
     * Get x
     * 
     * @return x
     */
    @javax.annotation.Nonnull
    public Double getX() {
        return x;
    }

    public void setX(@javax.annotation.Nonnull Double x) {
        this.x = x;
    }

    public UniverseStructureResponsePosition y(@javax.annotation.Nonnull Double y) {
        this.y = y;
        return this;
    }

    /**
     * Get y
     * 
     * @return y
     */
    @javax.annotation.Nonnull
    public Double getY() {
        return y;
    }

    public void setY(@javax.annotation.Nonnull Double y) {
        this.y = y;
    }

    public UniverseStructureResponsePosition z(@javax.annotation.Nonnull Double z) {
        this.z = z;
        return this;
    }

    /**
     * Get z
     * 
     * @return z
     */
    @javax.annotation.Nonnull
    public Double getZ() {
        return z;
    }

    public void setZ(@javax.annotation.Nonnull Double z) {
        this.z = z;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        UniverseStructureResponsePosition universeStructureResponsePosition = (UniverseStructureResponsePosition) o;
        return Objects.equals(this.x, universeStructureResponsePosition.x)
                && Objects.equals(this.y, universeStructureResponsePosition.y)
                && Objects.equals(this.z, universeStructureResponsePosition.z);
    }

    @Override
    public int hashCode() {
        return Objects.hash(x, y, z);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class UniverseStructureResponsePosition {\n");
        sb.append("    x: ").append(toIndentedString(x)).append("\n");
        sb.append("    y: ").append(toIndentedString(y)).append("\n");
        sb.append("    z: ").append(toIndentedString(z)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("x", "y", "z"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("x", "y", "z"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement
     *            JSON Element
     * @throws IOException
     *             if the JSON Element is invalid with respect to
     *             UniverseStructureResponsePosition
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!UniverseStructureResponsePosition.openapiRequiredFields.isEmpty()) { // has
                                                                                      // required
                                                                                      // fields
                                                                                      // but
                                                                                      // JSON
                                                                                      // element
                                                                                      // is
                                                                                      // null
                throw new IllegalArgumentException(
                        String.format(
                                "The required field(s) %s in UniverseStructureResponsePosition is not found in the empty JSON string",
                                UniverseStructureResponsePosition.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!UniverseStructureResponsePosition.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(
                        String.format(
                                "The field `%s` in the JSON string is not defined in the `UniverseStructureResponsePosition` properties. JSON: %s",
                                entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the
        // JSON string
        for (String requiredField : UniverseStructureResponsePosition.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s", requiredField,
                        jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!UniverseStructureResponsePosition.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes
                             // 'UniverseStructureResponsePosition' and its
                             // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<UniverseStructureResponsePosition> thisAdapter = gson.getDelegateAdapter(this,
                    TypeToken.get(UniverseStructureResponsePosition.class));

            return (TypeAdapter<T>) new TypeAdapter<UniverseStructureResponsePosition>() {
                @Override
                public void write(JsonWriter out, UniverseStructureResponsePosition value) throws IOException {
                    JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                    elementAdapter.write(out, obj);
                }

                @Override
                public UniverseStructureResponsePosition read(JsonReader in) throws IOException {
                    JsonElement jsonElement = elementAdapter.read(in);
                    validateJsonElement(jsonElement);
                    return thisAdapter.fromJsonTree(jsonElement);
                }

            }.nullSafe();
        }
    }

    /**
     * Create an instance of UniverseStructureResponsePosition given an JSON
     * string
     *
     * @param jsonString
     *            JSON string
     * @return An instance of UniverseStructureResponsePosition
     * @throws IOException
     *             if the JSON string is invalid with respect to
     *             UniverseStructureResponsePosition
     */
    public static UniverseStructureResponsePosition fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, UniverseStructureResponsePosition.class);
    }

    /**
     * Convert an instance of UniverseStructureResponsePosition to an JSON
     * string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
